/**
 * Block Editor JavaScript
 *
 * Scripts for custom blocks in the Gutenberg editor
 *
 * @package D_Theme
 * @since 1.0.0
 */

const { registerBlockType } = wp.blocks;
const { InspectorControls } = wp.blockEditor;
const { PanelBody, TextControl, ColorPicker } = wp.components;

(function() {
    'use strict';

    // Block editor initialization code here
    console.log('Block editor scripts loaded');

})();
